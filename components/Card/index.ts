import styled from 'styled-components'

export const Card = styled.div`
  font-weight: 400;
  height: 40em;
  background-color: white;
  border-radius: 8px;
  box-shadow: 0 0 transparent, 0 0 transparent, 0 25px 50px -12px rgba(0, 0, 0, 0.25);
`

export const Head = styled.div`
  font-weight: 400;
  padding: 1em;
  border-bottom: 1px solid rgba(0, 0, 0, 0.05);
`

export default Card
